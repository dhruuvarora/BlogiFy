<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head') %>
    <title>Add New Blog</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-whT+qkGZID8ht2HR0VzmFQ65U7ZxsnubOPl1B8mQNDD3mOQmKqJZsjl1ni2H7u8"
      crossorigin="anonymous"
    />
    <style>
      body {
        background-color: #f4f4f4;
        font-family: 'Arial', sans-serif;
      }

      .container {
        max-width: 800px;
        margin-top: 50px;
        padding: 20px;
        background-color: #fff;
        box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
        border-radius: 10px;
      }

      .form-title {
        font-size: 2rem;
        font-weight: bold;
        text-align: center;
        margin-bottom: 30px;
      }

      .form-label {
        font-weight: bold;
      }

      .btn-primary {
        background-color: #5e60ce;
        border: none;
        width: 100%;
      }

      .btn-primary:hover {
        background-color: #4d50b2;
      }

      .file-label {
        color: #6c757d;
      }

      .footer {
        text-align: center;
        margin-top: 20px;
        color: #6c757d;
      }
    </style>
  </head>
  <body>
    <%- include('./partials/nav') %>

    <div class="container">
      <h1 class="form-title">Add a New Blog Post</h1>

      <form action="/blog" method="post" enctype="multipart/form-data">
        <div class="mb-3">
          <label for="coverImage" class="form-label">Cover Image</label>
          <input
            type="file"
            class="form-control"
            id="coverImage"
            name="coverImage"
            aria-describedby="coverImage"
          />
          <div class="file-label">Choose a high-quality image that represents your blog.</div>
        </div>

        <div class="mb-3">
          <label for="title" class="form-label">Title</label>
          <input
            type="text"
            class="form-control"
            id="title"
            name="title"
            aria-describedby="title"
            placeholder="Enter your blog title here"
            required
          />
        </div>

        <div class="mb-3">
          <label for="body" class="form-label">Blog Content</label>
          <textarea
            name="body"
            class="form-control"
            id="body"
            rows="10"
            placeholder="Write your blog content here..."
            required
          ></textarea>
        </div>

        <div class="mb-3">
          <button class="btn btn-primary">Submit Blog</button>
        </div>
      </form>

      <div class="footer">
        <p>Blogging made easy with BlogiFy</p>
      </div>
    </div>

    <%- include('./partials/scripts') %>
  </body>
</html>
